<head>
    <%- include('../partials/head') %>
        <title>Editar Prode</title>
</head>


<body>
    <%- include('../partials/header') %>
        <main>
            <h1 id="titulo">Crear partidos para el prode: <%=prode.nombre%>
            </h1>
            <div class="crearEquipos">
                <% for( let i=0; i < prode.cantgrupos; i++ ) { %>
                    <div class="grupo">
                        <% for( let j=0; j < prode.cantequipos; j++ ) { %>
                            <% if (prode.grupos[i].partidos[j]) { %>
                                <div id="partido">
                                    <h2>grupo: <%=i+1%> partido: <%= j+1 %></h2>
                                    <form action="/prode/<%=i%>/<%=j%>/<%=prode.id%>" method="POST" >
                                        <div class="equipos">
                                            <img src="/Imagenes/<%= locals.prode.grupos[i].partidos[j].local  %>.png" alt="" height="40px" width="40px">
                                            <h1><%=prode.grupos[i].partidos[j].apodoLocal%> </h1>
                                            <% if (prode.grupos[i].partidos[j].resultado[0] == "x" ) { %>
                                                <input type="number" name="local"> 
                                            <% } else {%>  
                                                <h1><%=prode.grupos[i].partidos[j].resultado[0]%></h1>
                                                <% } %>                                          <h1>vs</h1>
                                            <% if (prode.grupos[i].partidos[j].resultado[0] == "x" ) { %>
                                                <input type="number" name="visitante"> 
                                            <% } else {%>
                                                <h1><%=prode.grupos[i].partidos[j].resultado[1]%></h1>
                                                <% } %>
                                            <h1><%=prode.grupos[i].partidos[j].apodoVisitante%> </h1>
                                            <img src="/Imagenes/<%= locals.prode.grupos[i].partidos[j].visitante  %>.png" alt="" height="40px" width="40px">
                                        </div>
                                        <div class = "info">
                                            <h1>Hora: <%=prode.grupos[i].partidos[j].hora%></h1>
                                            <h1>Fecha: <%=prode.grupos[i].partidos[j].fecha%></h1>
                                            <h1>Estadio: <%=prode.grupos[i].partidos[j].estadio%></h1>
                                        </div>
                                        <% if (prode.grupos[i].partidos[j].resultado[0] == "x" ) { %>
                                            <button type="submit"> ENVIAR</button>
                                        <% } else { %>
                                            <button type="submit"> EDITAR</button>
                                            <% } %>

                                    </form>
                                </div>
                                <% } else { %>
                                    <form method="POST"
                                        action="/prode/crearPartido?prode=<%=prode.id %> &grupo=<%=i+1%>&partido=<%=j+1%>"
                                        class="partido">

                                        <div class="equipo">
                                            <h2>grupo: <%=i+1%> partido: <%= j+1 %>
                                            </h2>
                                            <br>
                                            <div class="partido">
                                                <select name="local">
                                                    <option>Equipo 1</option>
                                                    <% for( let k=0; k < prode.grupos[i].cantequipos; k++ ) { %>
                                                        <option>
                                                            <%=prode.grupos[i].equipos[k].nombre %>
                                                        </option>
                                                        <% } %>

                                                </select>
                                                <h4>VS</h4>
                                                <select name="visitante">
                                                    <option>Equipo 2</option>
                                                    <% for( let k=0; k < prode.grupos[i].cantequipos; k++ ) { %>
                                                        <option>
                                                            <%=prode.grupos[i].equipos[k].nombre %>
                                                        </option>

                                                        <% } %>

                                                </select>
                                            </div>
                                            <br>
                                            <label for="estadio">Estadio:</label>
                                            <input type="text" name="estadio">
                                            <label for="fecha">Fecha:</label>
                                            <input type="date" name="fecha">
                                            <label for="hora">Horario:</label>
                                            <input type="time" name="hora">
                                            <button type="submit">Crear</button>

                                        </div>
                                    </form>

                                    <% } %>




                                        <% } %>

                    </div>

                    <% } %>
            </div>
            <div class = "eliminatorias" style = "text-align: center; margin-top: 20px;">
                <h1>Crear fase eliminatoria para el prode <%= prode.id  %> </h1>
            </div>
            <form action="/prode/eliminatorias?id=<%= prode.id %>" method="POST">
            <div class="main_cuadros" >
                <p class="descripcion_cuadros">RESULTADOS COPA AMERICA:</p>
                <div class="cuadro">
                    <div class="cuadros" id="cuadros">
                        <div class="ronda_1">
                            <p class="cuartos">Cuartos de Final:</p>
                            <div class="partido_cuadros">
                                <div class="equipos_cuadros">
                                    <div class="pais">
                                        <select name = "cuartos1Local">
                                            <option>Equipo 1</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                              
                                                <% } %>                                
                                            <% } %>              
                                        </select>
                                    </div>
                                    <div>
                                        <select name = "cuartos1Visitante">
                                            <option>Equipo 2</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                  
                                                <% } %>                             
                                            <% } %>                                          
                                        </select>     
                                    </div>
                                </div>
                                <div class="resultado_cuadros"> - </div>
                                <div class="border_cuadros">
                                    <div class="palito_cuadro">

                                    </div>
                                </div>
                            </div>
                            <div class="partido_cuadros">
                                <div class="equipos_cuadros">
                                    <div class="pais">
                                        <select name = "cuartos2Local">
                                            <option>Equipo 1</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                              
                                                <% } %>                                
                                            <% } %>              
                                        </select>
                                    </div>
                                    <div>
                                        <select name = "cuartos2Visitante">
                                            <option>Equipo 2</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                  
                                                <% } %>                             
                                            <% } %>                                          
                                        </select>     
                                    </div>
                                </div>
                                <div class="resultado_cuadros"> - </div>
                                
                            </div>
                        </div>
                        <div class="ronda_2">
                            <p class="semis">Semi-Final:</p>
                            <div class="partido_cuadros">
                                <div class="equipos_cuadros">
                                    <div class="pais">
                                        <p>Cuartos 1</p>
                                        <img src="/Imagenes/a-jugar.png" alt="">
                                    </div>
                                    <div>
                                        <p>Cuartos 2</p>
                                        <img src="/Imagenes/a-jugar.png" alt="">
                                    </div>
                                </div>
                                <div class="resultado_cuadros"> - </div>
                                <div class="border_cuadros_semi">
                                    <div class="palito_cuadro_semi">

                                    </div>
                                </div>
                            </div>
                            
                        </div>
                    </div>
                    <div class="cuadros_dos">
                        <div class="ronda_1">
                            <div class="partido_cuadros">
                                <div class="equipos_cuadros">
                                    <div class="pais">
                                        <select name = "cuartos3Local">
                                            <option>Equipo 1</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                              
                                                <% } %>                                
                                            <% } %>              
                                        </select>
                                    </div>
                                    <div>
                                        <select name = "cuartos3Visitante">
                                            <option>Equipo 2</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                  
                                                <% } %>                             
                                            <% } %>                                          
                                        </select>     
                                    </div>
                                </div>
                                <div class="resultado_cuadros"> - </div>
                                <div class="border_cuadros">
                                    <div class="palito_cuadro">

                                    </div>
                                </div>
                            </div>
                            <div class="partido_cuadros">
                                <div class="equipos_cuadros">
                                    <div class="pais">
                                        <select name = "cuartos4Local">
                                            <option>Equipo 1</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                              
                                                <% } %>                                
                                            <% } %>              
                                        </select>
                                    </div>
                                    <div>
                                        <select name = "cuartos4Visitante">
                                            <option>Equipo 2</option>
                                            <% for( let i = 0; i < prode.cantgrupos; i++ ) { %>
                                                <% for( let j = 0; j < prode.cantequipos/2; j++ ) { %>
                                                   <option> <%= prode.grupos[i].equipos[j].nombre  %> </option>                                  
                                                <% } %>                             
                                            <% } %>                                          
                                        </select>     
                                    </div>
                                </div>
                                <div class="resultado_cuadros"> - </div>
                            </div>
                        </div>
                        <div class="ronda_2_2">
                            <div class="partido_cuadros">
                                <div class="equipos_cuadros">
                                    <div class="pais">
                                        <p>Cuartos 3</p>
                                        <img src="/Imagenes/a-jugar.png" alt="">
                                    </div>
                                    <div>
                                        <p>Cuartos 4</p>
                                        <img src="/Imagenes/a-jugar.png" alt="">
                                    </div>
                                </div>
                                <div class="resultado_cuadros"> - </div>
                            </div>
                            
                        </div>
                        <div class="ronda_2">
                            <div class="Final">
                                <p class="descripcion_final">GRAN FINAL:</p>
                                <div class="equipos_cuadros">
                                    <div class="pais">
                                        <p>Semis 1</p>
                                        <img src="/Imagenes/a-jugar.png" alt="" height="60px"> 
                                    </div>
                                    <div>
                                        <p>Semis 2</p>
                                        <img src="/Imagenes/a-jugar.png" alt="" height="60px">
                                    </div>
                                </div>
                                <div class="resultado_cuadros"> - </div>
                            </div>
                            
                        </div>
                    </div>
                </div>
            </div>
            <button type="submit" style="width: 200px; margin: 20px; " >Crear</button>
        </form>
        
        </main>
</body>